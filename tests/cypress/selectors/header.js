export const header = '.bluehost-header';

export const nav = `.navtabs-desktop`;

export const logoLink = `${header} a[aria-label="Bluehost Logo linking to the Bluehost Control Panel"]`;

export const logo = `${logoLink} svg`;

export const helpLink = `${header} .bluehost-nav-wrap-element.help a`;

export const helpIcon = `${helpLink} svg`;

export const userMenu = `#bluehost-nav-popup-inner ul`;

export const userMenuToggle = `${header} .bluehost-nav-wrap-element.user-menu`;

export const userMenuIcon = `${userMenuToggle} svg`;