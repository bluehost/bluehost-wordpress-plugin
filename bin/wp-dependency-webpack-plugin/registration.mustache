<?php
/**
 * {{config.handlePrefix}}
 * Build: {{hash}}
 */

{{#manifest}}
/**
 * ENTRY: {{entry}}
 */
{{#assets.js}}
 wp_register_script(
     '{{kebab}}',
     {{{config.buildDirUrl}}} . '{{filename}}',
     apply_filters( '{{snake}}_js_deps', array({{#dependencies}}'{{.}}', {{/dependencies}}) ),
     false,
     true
 );
 ${{camel}}Data = apply_filters( '{{snake}}_data', array() );
 if ( ! empty( ${{camel}}Data ) ) {
    \wp_localize_script(
        '{{kebab}}',
        '{{camel}}',
        ${{camel}}Data
    );
 }
{{/assets.js}}
{{#assets.css}}
wp_register_style(
    '{{kebab}}',
     {{{config.buildDirUrl}}} . '{{filename}}',
    apply_filters( '{{snake}}_css_deps', array({{#dependencies}}'{{.}}', {{/dependencies}}) ),
    false
);
{{/assets.css}}

{{/manifest}}
// auto-generated by wp-dependency-webpack-plugin
